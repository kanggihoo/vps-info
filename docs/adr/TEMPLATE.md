# ADR-NNNN: {간결한 제목}

- 상태: 제안
- 결정일: YYYY-MM-DD
- 관련: {선행 ADR 번호, 이슈 키. 없으면 이 줄 삭제}

> 상태 값은 `제안` / `수용` / `폐기` / `대체됨(ADR-NNNN)` 중 하나를 쓴다.
> 번호는 영구 식별자다. 삭제된 ADR의 번호는 재사용하지 않는다.
> 이 안내 블록은 실제 ADR에서 삭제한다.

## 배경

{왜 이 결정이 필요했는지. 제약 조건, 요구사항, 현재 코드의 한계.
지금 코드가 어떤 상태여서 이 결정을 하게 됐는지 구체적으로. 파일·줄 번호를 적는다.}

## 결정

{무엇을 결정했는지. 항목이 여럿이면 번호를 매겨 각각 한 줄로 명확하게.}

1. **{항목}**: {결정 내용}
2. **{항목}**: {결정 내용}

## 검토한 대안들

> 진짜 대안이 있었을 때만. 자명하면 이 섹션 삭제.

### {대안 A}

- 장점:
- 단점:
- 기각 사유:

## 이유

{왜 이 선택이 대안보다 나은지. 트레이드오프를 명시적으로.}

## 결과

> "이 결정으로 파생되는 영향과 제약"만. 백로그는 여기 쓰지 않는다 (→ `decisions-later.md`).

- {감수하게 되는 제약}
- {가능해지는 것}
